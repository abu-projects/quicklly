# Quicklly Production Flow Protocol

- Preserve the production user flow exactly: keep the existing route sequence, checkout steps, navigation destinations, and journey order.
- Fix broken behavior, state, validation, accessibility, and presentation within that flow.
- Do not redesign, reorder, remove, or introduce production-flow steps unless the user explicitly requests a flow change.
- Treat this constraint as an invariant for every task in this workspace.
